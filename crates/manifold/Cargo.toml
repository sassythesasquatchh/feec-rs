[package]
name = "manifold"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
publish.workspace = true
description = "Simplicial topology and geometry for the formoniq thesis fork"

[dependencies]
common = { path = "../common" }
exterior = { path = "../exterior" }

nalgebra = "0.33.2"
nalgebra-sparse = "0.10.0"

indexmap = "2.8.0"
itertools = "0.14.0"
tracing = "0.1.41"

mshio = "0.4.2"
approx = "0.5.1"
rayon = "1.10.0"
